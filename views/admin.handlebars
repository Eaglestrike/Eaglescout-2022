<nav class="blue darken-2">
    <div class="nav-wrapper container">
        <div class="col s12">
            <a href="/admin" class="breadcrumb">Admin</a>
            <a href="/admin" class="breadcrumb">Dashboard</a>
        </div>
    </div>
</nav>

{{#if success_msg}}
<div class="section no-pad-bot" id="index-banner">
    <div class="container">
        <div class="row">
            <div class="info-message green white-text">
                {{success_msg}}
            </div>
        </div>
    </div>
</div>
{{/if}}

{{#if error_msg}}
<div class="section no-pad-bot" id="index-banner">
    <div class="container">
        <div class="row">
            <div class="info-message red darken-1 white-text">
                {{error_msg}}
            </div>
        </div>
    </div>
</div>
{{/if}}

{{#if error}}
<div class="section no-pad-bot" id="index-banner">
    <div class="container">
        <div class="row">
            <div class="info-message red darken-1 white-text">
                {{error}}
            </div>
        </div>
    </div>
</div>
{{/if}}

<div class="section no-pad-bot" id="index-banner">
	<div class="container center">
		<h1 class="header center blue-text text-darken-4">Eaglescout<span class="bullet"> &bull; </span><span class="subtitle">Admin Dashboard</span></h1>
        <br>
        <a class="waves-effect waves-light btn-large teal darken-1" href="/admin/register"><i class="material-icons left">account_box</i>Register User</a>
        <a class="waves-effect waves-light btn-large green darken-1" href="/admin/bulkimport"><i class="material-icons left">insert_drive_file</i>Bulk Import Emails</a>
        <a class="waves-effect waves-light btn-large blue darken-1" href="/admin/userlist"><i class="material-icons left">account_box</i>View/Edit Users</a>
        <br><br>
        <a class="waves-effect waves-light btn-large orange darken-1" href="/admin/event"><i class="material-icons left">edit</i>Change Event</a>
	    <a class="waves-effect waves-light btn-large red darken-1 modal-trigger" href="#confirm-clear-modal"><i class="material-icons left">not_interested</i>Clear Observations</a>
	</div>
</div>

<div id="confirm-clear-modal" class="modal">
	<div class="modal-content">
		<h4>Are you sure you want to clear all observations?</h4>
		<p>This action cannot be undone. It is recommended that you "Export Observations" before clearing.</p>
        <h1 class="header text-darken-4 med-text no-padding">Select Events To Export</h1>
        <select id="select-events" multiple>
            {{#if events}}
                {{#each events}}
                    {{#if current }}
                        <option value="{{ key }}" selected>{{ name }}</option>
                    {{else}}
                        <option value="{{ key }}">{{ name }}</option>
                    {{/if}}
                {{/each}}
            {{/if}}
        </select>
    </div>
	<div class="modal-footer">
    <button id="export-csv" class="btn waves-effect waves-light red">Export!</button>
	<a href="/admin/clearobservations" class="modal-close waves-effect waves-red btn-flat">Clear Observations</a>
	</div>
</div>